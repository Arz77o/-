VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ============================================================
# متغيرات Meta CAPI — لا تُضاف هنا إطلاقًا!
# هذا الملف ينتهي به المطاف على المتصفح (client-side)، وأي شيء
# هنا يصبح عامًا وقابلاً للقراءة من أي زائر لموقعك.
#
# بدلًا من ذلك، أضف هذه المتغيرات من Supabase Dashboard مباشرة:
# Project Settings → Edge Functions → Secrets
#
#   META_PIXEL_ID            = 1698562094890502 (نفس البكسل القديم)
#   META_ACCESS_TOKEN         = (من Events Manager → Settings → Conversions API)
#   PURCHASE_TRIGGER_SECRET   = (نص عشوائي طويل تختاره أنت، يحمي حدث Purchase تحديدًا)
#   META_TEST_EVENT_CODE      = (اختياري، فقط أثناء الاختبار عبر Test Events tab)
#
# ملاحظة: لا حاجة لأي "رمز سري عام" لحدث Lead — Supabase تتحقق
# تلقائيًا أن المستخدم يحمل anon key صالحًا (verify_jwt)، وهذا كافٍ.
# ============================================================
